DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'zitatech_dev',  # Replace with your local database name
        'USER': 'zitatech',  # Replace with your PostgreSQL username
        'PASSWORD': 'rm#122', # Replace with your PostgreSQL password
        'HOST': 'localhost',  # Or '127.0.0.1' if localhost causes issues
        'PORT': '5432',  # Default PostgreSQL port
    }
}
